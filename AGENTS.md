# AGENTS.md

# HeartQuest 開発ルール

このファイルは、HeartQuestプロジェクトを開発するAIエージェントおよび開発者が従うべきルールを定義する。

HeartQuestにおける最優先事項は、README.mdで定義されたプロダクトを、ハッカソンで実際に動作する状態まで完成させることである。

高度な設計、新しい技術の導入、過度なリファクタリング、将来を想定した複雑な拡張よりも、

**「HeartQuestの主要なユーザー体験が最後まで正常に動作すること」**

を最優先する。

---

# 1. 最優先原則

開発時は、以下の優先順位に従う。

1. HeartQuestがプロダクトとして正常に動作すること
2. README.mdに定義されたMVPを完成させること
3. 既存機能を壊さないこと
4. ユーザーから指示された変更を正確に実装すること
5. シンプルで理解しやすい実装を維持すること
6. 追加機能や改善を検討すること

技術的に高度であることや、コードが理論上もっと美しくなることは、プロダクト完成より優先しない。

---

# 2. README.mdを仕様の基準とする

HeartQuestの基本仕様、技術構成、通信構成、機能、API、データベース構成、優先順位については、原則としてリポジトリ直下の `README.md` を基準とする。

実装時には必要に応じてREADME.mdを確認し、README.mdで定義された方針から逸脱しないこと。

特に以下についてはREADME.mdの内容を尊重する。

* システム構成
* 技術スタック
* Firebase Authenticationによる認証方式
* Cloudflare Tunnelによる外部公開
* FastAPIのAPI構成
* SQLiteのデータ構成
* AIによる回復方法提案・分析
* 機能の優先順位
* MVP
* ディレクトリ構成
* アプリ全体の通信フロー

README.mdとユーザーの最新の明示的な指示が矛盾する場合は、ユーザーの最新の指示を優先する。

ただし、影響が大きい変更については実装前にユーザーへ伝えること。

---

# 3. HeartQuestの目的

HeartQuestは、ユーザーが日々行ったリフレッシュ・回復行動を記録し、その効果を評価・分析するWebアプリケーションである。

中心となるユーザー体験は以下である。

```text
ログイン
   ↓
回復方法を選ぶ / AIに提案してもらう
   ↓
回復行動を実施
   ↓
10段階評価
   ↓
AI分析
   ↓
履歴に保存
   ↓
過去の回復効果を確認
```

開発中に判断に迷った場合、この一連の体験を完成させるために必要かどうかを判断基準とする。

---

# 4. 使用する技術スタック

HeartQuestではREADME.mdで定義されている以下の技術を使用する。

## Frontend

* React
* TypeScript
* Vite
* CSS
* Tailwind CSS
* Chart.js
* Firebase JavaScript SDK

## Authentication

* Firebase Authentication
* Firebase Admin SDK

## Backend

* Python
* FastAPI

## Database

* SQLite

## AI

* README.mdで想定されているAI API

## Server

* Windows PC

## Network

* Cloudflare Tunnel
* cloudflared

## Development

* Git
* GitHub

---

# 5. 新しい技術スタックを勝手に追加しない

README.mdに記載されていない新しい技術スタックを、ユーザーの許可なく追加してはならない。

対象には以下を含む。

* 新しいフレームワーク
* 新しいデータベース
* 新しい認証サービス
* 新しいクラウドサービス
* 新しいホスティングサービス
* 新しい状態管理ライブラリ
* 新しいUIフレームワーク
* 新しいORM
* 新しいAPIフレームワーク
* 新しいグラフライブラリ
* 新しいネットワークサービス
* Docker等の新しい実行基盤
* CI/CDサービス
* その他、README.mdに存在しない主要な依存技術

「実装が楽になる」「一般的によく使われる」「将来的に便利」という理由だけで追加してはならない。

新しい技術が必要だと判断した場合は、実装せず、

1. なぜ必要なのか
2. 既存技術では実現できない理由
3. 追加によるメリット
4. 追加によるリスク
5. ハッカソン期間への影響

をユーザーへ説明し、明示的な承認を得ること。

---

# 6. 新しい依存パッケージを安易に追加しない

`npm install`、`pip install` 等によって新しい依存パッケージを追加する場合も慎重に扱うこと。

既存の技術スタックだけで実装できる場合は、新しいパッケージを追加しない。

新規パッケージが本当に必要な場合は、原則としてユーザーへ確認する。

特に、単純な処理を行うためだけに小規模ライブラリを追加することを避ける。

---

# 7. ユーザーが指示した範囲だけ変更する

変更内容は、ユーザーから依頼された範囲に留める。

例えばユーザーから、

```text
ログイン画面を修正してください
```

と指示された場合、

* 分析画面を変更する
* DB構造を変更する
* APIを全面的に整理する
* ディレクトリ構成を変更する
* 無関係なコードをリファクタリングする

といった変更を同時に行ってはならない。

---

# 8. 「ついでの変更」を行わない

以下のような変更を、ユーザーの指示なしで実施しない。

* コード全体のリファクタリング
* ファイル名変更
* ディレクトリ移動
* 変数名の大規模変更
* API名変更
* DBカラム名変更
* UIデザインの全面変更
* パッケージ更新
* 設定ファイル整理
* 不要と思われるファイルの削除
* コメントの一括変更
* フォーマッタによるリポジトリ全体の書き換え

必要な変更だけを、可能な限り小さい差分で実装する。

---

# 9. スコープ外の問題を発見した場合

作業中に、依頼内容とは関係のない問題を発見することがある。

その場合、勝手に修正しない。

以下のように扱う。

```text
問題を発見
   ↓
現在の依頼を進めるために修正が必須か確認
   ↓
必須ではない
   → 修正せずユーザーへ報告
   ↓
必須
   → 最小限の修正に留める
   → 変更理由をユーザーへ説明
```

---

# 10. ハッカソンで動くことを優先する

HeartQuestは短期間のハッカソン開発を前提としている。

そのため、

```text
完璧な設計
```

より、

```text
シンプルで確実に動く実装
```

を優先する。

以下のような過剰設計を避ける。

* 必要以上の抽象化
* 将来の拡張だけを目的とした複雑な設計
* 過度なクラス分割
* 不要なマイクロサービス化
* 不要なRepositoryパターン
* 不要なDIコンテナ
* 不要なキャッシュ層
* 不要なイベント駆動構成
* 不要なインフラ追加

現在必要な機能を、現在の技術構成で確実に動作させる。

---

# 11. 機能の優先順位を守る

README.mdに基づき、以下を優先する。

## 優先度：高

* Firebaseログイン
* 回復方法登録
* 10段階評価
* AIによる回復方法提案
* 回復履歴
* SQLiteへの保存
* Cloudflare Tunnelによる外部公開

これらはHeartQuestの基本体験を構成するため、最優先で正常動作させる。

## 優先度：中

* ブックマーク
* AIによる回復効果分析
* 回復方法ランキング
* 月ごとのグラフ
* 円グラフ

## 時間に余裕がある場合

* ゆるキャラ
* 気分バー
* キャラクターアニメーション
* AIスコアとキャラクター状態の連動

優先度の低い機能のために、優先度の高い機能の完成が遅れる変更を行ってはならない。

---

# 12. 認証方式を変更しない

ログインにはFirebase Authenticationを使用する。

FastAPIではFirebase Admin SDKを使用してFirebase ID Tokenを検証する。

基本的な通信は以下とする。

```text
React
   ↓
Firebase Authentication
   ↓
Firebase ID Token取得
   ↓
React
   ↓
Authorization: Bearer <Firebase ID Token>
   ↓
FastAPI
   ↓
Firebase Admin SDK
   ↓
Token検証
   ↓
Firebase UID取得
```

独自パスワード認証や独自セッション認証などを勝手に追加しない。

Windows PC側でユーザーのパスワードを管理しない。

---

# 13. 認証が必要なAPIを迂回しない

Cloudflare Tunnelで公開されたURLはインターネットからアクセス可能である。

そのため、ユーザーデータを扱うAPIではFirebase Authenticationによる認証を前提とする。

開発を簡単にする目的で、

* 認証処理を削除する
* 認証チェックをコメントアウトする
* 誰でもユーザーデータを取得できるAPIを作る

といった変更を行わない。

---

# 14. ユーザー識別にはFirebase UIDを使用する

FirebaseのユーザーIDである `uid` をHeartQuest側のユーザーと紐付ける。

README.mdで想定されている基本構造は以下である。

```text
users
--------------------------------
id
firebase_uid
display_name
created_at
```

ユーザー識別方式を別方式へ変更する場合は、事前にユーザーの承認を得る。

---

# 15. データベースはSQLiteを使用する

HeartQuestのアプリケーションデータはSQLiteへ保存する。

ユーザーの指示なしで、

* PostgreSQL
* MySQL
* MongoDB
* Firestore
* Supabase
* その他のDB

へ変更しない。

README.mdで想定されている主要テーブルを尊重する。

## users

```text
id
firebase_uid
display_name
created_at
```

## recoveries

```text
id
user_id
activity
memo
rating
ai_score
ai_comment
created_at
```

## bookmarks

```text
id
user_id
title
description
created_at
```

既存データとの互換性を壊すDB変更は特に慎重に扱う。

---

# 16. API構成を尊重する

README.mdでは以下のFastAPIエンドポイントが想定されている。

```text
/auth
└── GET /auth/me

/recoveries
├── GET  /recoveries
├── POST /recoveries
└── PUT  /recoveries/{id}/rating

/bookmarks
├── GET    /bookmarks
├── POST   /bookmarks
└── DELETE /bookmarks/{id}

/ai
├── POST /ai/recommend
└── POST /ai/analyze

/analytics
├── GET /analytics/ranking
└── GET /analytics/monthly
```

ユーザーから変更指示がない限り、既存APIの名前、役割、HTTPメソッドを不必要に変更しない。

---

# 17. AI機能の目的を維持する

AIによる回復方法提案では、

* 定番の回復方法
* ユーザーがまだ試していない新規性のある回復方法

の両方を提案する。

過去の回復履歴を利用し、ユーザーがすでに実施した方法だけを繰り返し提案することを避ける。

AIによる回復効果分析では、README.mdで想定されている以下の情報を利用する。

```text
回復前の状態
+
実施した回復方法
+
回復後の感想
+
本人の10段階評価
```

AI機能そのものを複雑化するより、HeartQuestのユーザー体験として一連の流れが正常に動くことを優先する。

---

# 18. 10段階評価を維持する

ユーザーによる回復効果の評価は1〜10の10段階を基本とする。

ユーザーから指示がない限り、

* 5段階評価
* 星評価
* 100点評価

などへ変更しない。

入力値は必要に応じてFastAPI側でも検証し、不正な値を保存しない。

---

# 19. 外部公開方式を変更しない

Windows PC上のHeartQuestを外部公開するためにCloudflare Tunnelと `cloudflared` を使用する。

審査員やプレイヤーは、Tailscale、Cloudflare One Client / WARP、Cloudflareアカウントへの参加を必要とせず、通常のブラウザから公開URLへアクセスできる構成とする。

原則として、Cloudflareで管理する公開ホスト名を次のローカルサービスへ接続する。

```text
フロントエンド公開ホスト名
→ http://localhost:5173 など、実際のフロントエンドポート

API公開ホスト名
→ http://localhost:8000 など、実際のFastAPIポート
```

実際のポート番号や起動方法がプロジェクト設定で定義されている場合は、その設定を優先する。

公開構成を変更する際は、以下を守る。

* ルーターのポート開放を行わない
* 固定グローバルIPやDDNSを前提にしない
* TailscaleまたはTailscale Funnelへ戻さない
* 審査員端末へのVPNクライアント導入を前提にしない
* Cloudflare Accessによる追加ログインを、ユーザーの許可なく必須にしない
* Cloudflare Tunnelを迂回してFastAPI、Vite、SQLiteなどを直接インターネットへ公開しない
* VPS、AWS、Azure、Google Cloud、その他の公開方法へ無断で変更しない

公開URLはインターネットから到達可能であるため、Firebase Authenticationによるアプリケーション認証を維持する。

---

# 20. ディレクトリ構成を尊重する

README.mdでは以下の構成が想定されている。

```text
heartquest/
│
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   ├── pages/
│   │   ├── services/
│   │   └── App.tsx
│   ├── package.json
│   └── vite.config.ts
│
├── backend/
│   ├── main.py
│   ├── api/
│   │   ├── auth.py
│   │   ├── recoveries.py
│   │   ├── bookmarks.py
│   │   ├── ai.py
│   │   └── analytics.py
│   ├── services/
│   │   ├── firebase.py
│   │   └── ai.py
│   └── database/
│       ├── database.py
│       └── heartquest.db
│
├── .env
├── .gitignore
└── README.md
```

実際のリポジトリ構成がREADME.mdから変化している場合は既存構成を確認したうえで作業する。

依頼に必要がない限り、大規模なファイル移動やディレクトリ再編を行わない。

---

# 21. 秘密情報をGitへ含めない

以下の情報をGitリポジトリへコミットしてはならない。

* APIキー
* AI APIキー
* Firebase秘密鍵
* Firebase Admin SDKのサービスアカウント秘密情報
* アクセストークン
* Cloudflare Tunnelのトークン
* `cert.pem` およびトンネル認証情報のJSONファイル
* パスワード
* 個人用認証情報
* `.env` に保存される秘密情報
* その他のCredential

秘密情報は環境変数等を利用して管理する。

`.gitignore` の対象になっている秘密ファイルを無断で追跡対象へ変更しない。

---

# 22. `.env` を慎重に扱う

`.env` に保存された値を、

* README.md
* ソースコード
* ログ
* コミットメッセージ
* Issue
* Pull Request

などへコピーしない。

秘密値が必要な場合でも、値そのものを出力しない。

---

# 23. Git操作はユーザーの承認を得てから実行する

Gitの状態を変更する操作は、原則としてユーザーから明示的な承認を得てから実行する。

特に以下の操作は勝手に行ってはならない。

```bash
git add
git commit
git push
git pull
git merge
git rebase
git reset
git revert
git stash
git checkout
git switch
git branch
git tag
```

GitHub上で同等の状態変更を行う操作も同様とする。

例：

* Commit作成
* Push
* Branch作成
* Branch削除
* Pull Request作成
* Pull Request更新
* Merge
* Rebase
* Tag作成
* ファイルの直接作成・更新・削除

これらを実施する前に、

1. 何を変更したか
2. どのファイルが変更対象か
3. Git上で何を実行しようとしているか

をユーザーへ提示し、承認を得る。

---

# 24. 読み取り専用のGit操作は実行可能

状態確認のための読み取り専用操作は、必要に応じて実行してよい。

例：

```bash
git status
git diff
git log
git show
git remote -v
```

これらの操作ではリポジトリの状態を変更しないこと。

---

# 25. GitHubへの変更を自動的に反映しない

コードの修正が完了しても、それだけを理由にCommitやPushを実行してはならない。

基本フローは以下とする。

```text
ユーザーから依頼
   ↓
対象を確認
   ↓
実装
   ↓
動作・差分確認
   ↓
変更内容をユーザーへ報告
   ↓
Commit / Pushしてよいか確認
   ↓
ユーザー承認
   ↓
Git操作
```

ユーザーから承認が得られるまでは、変更内容を保持した状態で停止する。

---

# 26. 破壊的操作は禁止

明示的な指示なしに以下の操作を行わない。

```bash
git reset --hard
git clean -fd
git push --force
git push --force-with-lease
```

また、以下も勝手に実施しない。

* DBファイル削除
* テーブル削除
* ユーザーデータ削除
* リポジトリ削除
* ブランチ削除
* 履歴書き換え
* 大量ファイル削除

破壊的操作が必要な場合は、影響を説明してユーザーの明示的な承認を得る。

---

# 27. 既存コードを先に確認する

新しい実装を開始する前に、関連する既存コードを確認する。

確認せずに、

* 同じ機能を重複実装する
* 同じAPIを別名で追加する
* 同じユーティリティを複数作る
* 既存処理を無視して新方式を作る

ことを避ける。

可能な限り既存のコードと設計を利用する。

---

# 28. 最小変更を基本とする

バグ修正・機能追加ともに、必要な範囲で最も小さい変更を優先する。

例えば1ファイルの修正で解決できる問題に対して、不必要に10ファイルを変更しない。

変更ファイル数を減らすこと自体が目的ではないが、変更範囲が広がるほど既存機能を壊すリスクが高くなることを意識する。

---

# 29. 既存機能を壊さない

変更後は、可能な範囲で関連する既存機能への影響を確認する。

特に以下の流れを重視する。

```text
ログイン
↓
認証付きAPIアクセス
↓
回復方法登録
↓
評価
↓
AI処理
↓
SQLite保存
↓
履歴表示
```

一つの機能を直すために、この主要フローの別部分を壊さないこと。

---

# 30. エラーを隠さない

エラーを解決する代わりに、

```python
try:
    ...
except:
    pass
```

のように問題を握りつぶす実装を安易に使用しない。

フロントエンドでも、

* API通信失敗
* 認証失敗
* AI API失敗
* DB処理失敗

などの重要な失敗を、ユーザーが何も分からない状態にしない。

ただし、ハッカソンという性質を考慮し、エラーハンドリングを必要以上に複雑にしない。

---

# 31. 型安全性と入力検証を維持する

フロントエンドではTypeScriptを利用し、安易に `any` を増やさない。

FastAPIでは可能な範囲でリクエストデータを検証する。

特に、

* ratingが1〜10か
* 必須項目が存在するか
* 認証ユーザーが正しいか
* 他ユーザーのデータへアクセスしていないか

を確認する。

---

# 32. ユーザーごとのデータを分離する

HeartQuestはFirebase UIDによってユーザーを識別する。

回復履歴、ブックマーク、分析結果などを取得・更新する場合、認証されたユーザー自身のデータだけを対象とする。

URLやリクエストのIDを書き換えるだけで他ユーザーのデータへアクセスできる実装にしてはならない。

---

# 33. AI APIはバックエンド経由で使用する

AI API用の秘密鍵をフロントエンドへ置かない。

基本的な通信は以下とする。

```text
React
   ↓
FastAPI
   ↓
SQLiteから必要な履歴を取得
   ↓
AI API
   ↓
FastAPI
   ↓
必要に応じてSQLiteへ保存
   ↓
React
```

AI APIの秘密情報をブラウザへ露出させない。

---

# 34. UI変更は機能完成を妨げない範囲にする

UIはユーザーがHeartQuestを使いやすくするために重要だが、基本機能の完成より優先しない。

特に、

* 過度なアニメーション
* 大規模なデザインシステム作成
* キャラクター演出
* 高度なトランジション

などに開発時間を使いすぎない。

まず主要な操作が分かりやすく、正常に利用できる状態を作る。

---

# 35. README.mdを勝手に書き換えない

README.mdはHeartQuestの設計基準となる重要なドキュメントである。

実装とREADME.mdに差異を発見した場合、勝手にREADME.mdを実装へ合わせて変更するのではなく、ユーザーへ報告する。

README.mdの変更を依頼された場合のみ、必要な箇所を更新する。

---

# 36. 不明点がある場合の判断

仕様が明確でない場合、以下の順番で判断する。

```text
1. ユーザーの最新の指示
        ↓
2. README.md
        ↓
3. 現在の実装
        ↓
4. 最小変更・最小複雑性の原則
```

複数の解釈によってプロダクトの動作や仕様が大きく変わる場合は、独断で決定せずユーザーへ確認する。

---

# 37. 実装完了時に報告する内容

作業完了時は、少なくとも以下をユーザーへ報告する。

* 何を変更したか
* 変更したファイル
* 実装した内容
* 動作確認した内容
* 未確認事項
* 注意点
* Git操作をまだ実行していない場合はその旨

複雑な変更の場合は、必要に応じて影響範囲も説明する。

---

# 38. Commit前の確認

Commitを行う前に以下を確認する。

* ユーザーの依頼範囲を超えていないか
* 不要なファイルが含まれていないか
* `.env` 等の秘密情報が含まれていないか
* デバッグ用コードが残っていないか
* 不要なログが残っていないか
* 主要機能を壊していないか
* 新しい技術を無断追加していないか
* ユーザーからCommitの承認を得ているか

---

# 39. Push前の確認

Pushを行う前に以下を確認する。

* Push対象のCommitが正しいか
* Push先のリポジトリが正しいか
* Push先のブランチが正しいか
* 他メンバーの作業を不必要に上書きしないか
* ユーザーからPushの承認を得ているか

HeartQuestは2人で共同開発するため、他メンバーの作業を尊重する。

---

# 40. Definition of Done

機能を「完成」と扱うためには、可能な範囲で以下を満たすこと。

```text
□ ユーザーの依頼内容を満たしている
□ README.mdの方針に反していない
□ 新しい技術を勝手に追加していない
□ 既存機能を壊していない
□ 認証が必要な処理で認証を維持している
□ ユーザーごとのデータ分離が維持されている
□ 秘密情報をコードへ含めていない
□ 不必要な変更を行っていない
□ 主要なエラーが握りつぶされていない
□ 可能な範囲で動作確認している
□ 変更内容をユーザーへ説明できる
```

---

# 41. 最後の判断基準

判断に迷った場合は、以下を自問する。

```text
この変更は、
HeartQuestをハッカソンで完成させ、
実際にユーザーが使える状態にするために
今、本当に必要か？
```

必要でない場合は追加しない。

HeartQuestの中心価値は、

**「自分に合った回復方法を見つけ、試し、その結果からさらに自分に合う方法を発見すること」**

である。

開発時間は、このユーザー体験を完成させるために優先して使用する。
